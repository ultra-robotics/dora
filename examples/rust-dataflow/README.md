# Quick Rust example

To get started:

```bash
cargo run --example rust-dataflow
```

## Trying `dora node stop` and `dora node start`

See [README-stop-restart.md](README-stop-restart.md) and `dataflow-stop-restart.yml` for a Python run-forever node you can stop and start manually.
